# M1 验收证据 · 双产物浏览器冒烟（2026-09-16）

- 环境：Ubuntu 20.04.6 · Python 3.12（uv venv）· Node v18.20.8 · Playwright 1.48.0 + Chromium（headless，1440×900）
- 被测产物：`build.py` 当次构建的中北（76 只，9 分片）与示例校（2 只，1 分片），均为**全离线单页 + assets**
- 服务方式：`python -m http.server` 指向产物目录（模拟 Pages 托管；file:// 直开同样可用）
- 复跑命令：
  ```bash
  python tools/build.py --pkg schools/nuc   --out /tmp/dist-root/nuc
  python tools/build.py --pkg schools/示例校 --out /tmp/dist-root/demo
  python tools/smoke_browser.py --root /tmp/dist-root --shots docs/screenshots
  ```
- 机器可读报告：[`smoke-report-20260916.json`](smoke-report-20260916.json)（`"ok": true`）

## 结果（7/7 通过，console error = 0）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| ① | 中北极光开场屏 + 底部原创署名行（含 GitHub） | ✅ | 署名行文本「原创普查数据 · 转载请署名 · github.com/TianWenzzzh/nuc-cat-starmap」，href 指向 nuc 仓库；[01_nuc_intro.png](screenshots/01_nuc_intro.png) |
| ② | 星图主界面渲染星点与分区 | ✅ | 运行时 `CATS.length=76`、`AREAS.length=12`；[02_nuc_starmap.png](screenshots/02_nuc_starmap.png) |
| ③ | 影廊灯箱能开、能切换、能关 | ✅ | 随机开图 CAT-044「圆眼 59/76」→ 下一张 CAT-045「黑豆 60/76」→ 关闭后 `display=none`；[03a](screenshots/03a_nuc_lightbox.png) / [03b](screenshots/03b_nuc_lightbox_next.png) |
| ④ | 本命猫卡 / 喵星护照面板 | ✅ | 更多菜单 →「本命猫测试」面板 `#soulP.open`、`display=flex`；[04_nuc_soul.png](screenshots/04_nuc_soul.png) |
| ⑤ | 分享卡导出图带署名 | ✅ | 下载 PNG 720×1120 / 864,706 字节（**带真实照片版**，canvas 未被污染）；底部绘制「原创 · github.com/TianWenzzzh/nuc-cat-starmap」；[05_nuc_poster.png](screenshots/05_nuc_poster.png) |
| ⑥ | 示例校 2 颗星渲染、无控制台错误 | ✅ | `CATS.length=2`、`CALIB=0`（走 basePos）；[06_demo_stars.png](screenshots/06_demo_stars.png) |
| ⑦ | 星位算法推导注记 | ✅（形态见下「事实修正」） | 源码注释「星位由页面内置算法按编号稳定推导（basePos）…」存在；无 CALIB 个体卡片 pill 显示「随机分布 · 待校准」；[07_demo_card_pill.png](screenshots/07_demo_card_pill.png) |

## Console 记录

两页均为 **0 条 error / 0 条 pageerror**，各 2 条 `log`：

1. `🐱 中北喵星图 · NUC CAT GALAXY v2.7`（示例校为 `示例校喵星图 · DEMO CAT GALAXY v1.0`）；
2. 原创署名块：`原创作品 © 2026 TianWenzzzh · N 只猫的实地普查档案 / 开源仓库：<repo> / 转载请署名并附仓库链接`。

无 warning。

## 语法门禁

`node tools/check_html_scripts.mjs template/starmap.html /tmp/nuc-dist /tmp/demo-dist`：
**3 个 HTML + 10 个 .js，共 19 块，失败 0**（模板占位符是合法 JS 标识符形态，模板本身也通过检查）。

## 事实修正（相对任务书 P4 措辞）

- 任务书 ⑦ 措辞为「页脚『星位为算法推导』注记」。实测 **v2.7 正式版没有可见页脚元素**（模板字节级往返门禁不允许新增任何字节），该注记的真实形态是：
  1. CALIB 上方的 JS 源码注释（中北：「固化校准坐标（2026-08-17 v6 …）」；示例校等无固化坐标的包：「星位由页面内置算法按编号稳定推导（basePos）· 可在页面上手拖校准后导出坐标」）；
  2. 无 CALIB 的个体，猫卡上有可见 pill「随机分布 · 待校准」——对陌生用户的实际告知效果等同页脚注记。
- 任务书 P3 预设 195 个照片键，实测 **76 键（75 张代表照 + 1 底图，1 张两猫共用）**；194 是「普查实拍总量」文案口径，CSV「照片数量」列合计 167。详见 `docs/token映射表.md` §4-bis。

## M1 硬标准对照

> 陌生用户不看代码，按 README 在 30 分钟内用示例校数据包产出可正常渲染的星图 HTML；中北 76 只全量产物通过浏览器冒烟。

- 示例校：2 行 CSV + 2 张占位图 + 1 张底图，`uv run --with pillow tools/build.py …` 一条命令产出，7 项冒烟全过；
- 中北：76 只全量（8.27 MB 数据包），字节级往返测试 + 76 照片数据行与 v2.7 源分片逐字节相同 + CALIB 54 键坐标全等 + 浏览器冒烟全过。
