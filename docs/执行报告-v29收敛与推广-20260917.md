# 执行报告：v29 三仓收敛 + 推广包（16 小时自主执行）

- 会话开始：2026-09-17 深夜（P0 基线），主体执行 2026-09-18（文件名沿用任务提示词的 20260917）
- 任务书：盘根《16小时自主执行提示词.md》；施工书：[T6-工厂与05收敛详细执行计划.md](T6-工厂与05收敛详细执行计划.md)
- 授权：提示词头部声明即 T6 F0 的 G1/G3 书面授权（G2/G4/G5 于 P0 实测复核）

## 结论（TL;DR）

**T6 施工书 F0–F7 全部完成，三仓收敛达成 DoD，三个版本已发布**：
工厂 **v1.1.0**（默认 v29 引擎）→ 05 **v2.8**（中北星图懒加载产物，与 16 托管逐字节一致）→
16 **v1.2.0**（反向 vendor 收口，渲染真相单源）。M2/M3 推广物料 7 项成稿（只备料未发布）。
测试全线只增不减：工厂 730→**747**，16 开源模板 70→**72**，05 门禁 11→**14 块**（懒加载分片 +1）。

## 发版清单（终版）

| 仓库 | 版本 | 位置 | 链接 |
|---|---|---|---|
| catgalaxy-factory | v1.1.0 | feat/v29-template `8284332`（双 OS CI+冒烟绿） | https://github.com/TianWenzzzh/catgalaxy-factory/releases/tag/v1.1.0 |
| nuc-cat-starmap | v2.8 | main `339b943` | https://github.com/TianWenzzzh/nuc-cat-starmap/releases/tag/v2.8 |
| meow-starmap | v1.2.0 | main `d013a17`（CI+Pages 双绿） | https://github.com/TianWenzzzh/meow-starmap/releases/tag/v1.2.0 |

## 终验（P8，2026-09-18）

| 项 | 结果 |
|---|---|
| 工厂全量 pytest（默认引擎 v29，含 v29 用例，无 --ignore） | **747/0/0/0**，exit 0 |
| 16 全量 unittest | **Ran 72 tests OK (skipped=2)** |
| 16 门禁 | 11 HTML + 11 js = **47 块，失败 0** |
| 05 门禁 | 1 HTML + 9 js = **14 块，失败 0** |
| 备份 bat 末次复跑 | bat_exit=0（robocopy 各段执行，零差异守卫通过） |
| 三仓 git status | 05 `## main...github/main` 干净｜14 `## feat/v29-template...origin/feat/v29-template` 干净｜16 `## main...origin/main` 干净 |
| 关键 CI | 工厂 35255218410 success（含 browser-smoke）；16 35257608474 success（CI+Pages） |

## P0｜开工基线 ✅

| 检查项 | 结果 | 证据 |
|---|---|---|
| 代理探活 / 16 仓 `2ef6b3a` 推送 | 200 OK；CI 双 job 绿 | run 35245226274 |
| 工厂 G5 | master=origin/master=`f13f1b1`；feat/v29-template 恰领先 1 提交 | git rev-parse |
| 工厂 venv 对齐 3.12 | CPython 3.12.12 重建 | `D:\venvs\catfactory` |
| 工厂基线 pytest | **730/0/0/0** | junitxml `$env:TEMP\p0.xml` |
| 05 tag / 门禁 | pre-v29、v2.7 在；11 块 0 失败 | git tag -l / node 门禁 |
| 16 基线 | 70 项 OK (skip=2)；47 块 0 失败 | unittest / gate |
| 备份（G4） | 212 文件两侧一致，FAILED 0 | bat_exit=0 |

## P1｜F2 工厂引入 v29 模板与共享渲染器 ✅（`1855752`）

- 修 checkpoint 已知坑：`app/starmap_render.py:179`（`{js(name)]` → `{js(name)}`）
- 渲染层**逐字节对齐 16 仓 build.py 口径**：v2.7 风格数据块（裸键+双引号值）、
  js_num/js_num3 去零、空值哨兵（`const CALIB={};` 等）、4/3/6 每行布局、
  `short` 剥「大学/学院/学校」后缀、`derive_*` 五算法对齐、`assign_anchors`
  复用 `star_mapper`（checkpoint 自创环形锚点版弃用）
- 在线侧安全：自由文本按**落地上下文**转义（html_esc/js_str，合法值恒等，
  不破坏与 16 的字节对齐）；`__PRODUCT__/__EN__/__VERSION__` 直通点位转义
- 引擎选择三入口（默认 v1→P4 切 v29）；`--engine v1` deprecated 出口保留
- 出门证据：全量 **743/0**（730 基线不降级 + 13 条 v29 用例）；模板与 16 仓
  sha256 同源（`22fdf19e…`）

## P2｜F3 双形态懒加载 ✅（`25ce965`）

- relative：模板加载器休眠（`__PHOTO_BOOT=null`），`PH(p)` 回退返回路径本身，
  **零分片零引导**；MAP_SRC=packager 固定落点 `assets/map.jpg`
- `photo_loading` 服务端透传（lazy|eager|relative；relative 形态强制 relative）
- `scripts/smoke_v29.py`：**四组合全绿**（inline/relative × http/file://）——
  inline 初始仅 `photo-data-00.js`、影廊按需取片解码；relative 全程零分片请求；
  0 pageerror。证据：`docs/验收证据-F3-双形态冒烟.md` + 4 截图

## P3｜F4 测试与 CI 加固 ✅（同 `25ce965`）

- 新增 ubuntu 专用 `browser-smoke` job（Chromium 成本取舍按施工书）；
  `test_ci.py` 锁定「冒烟 job 存在 + v29 套件存在」防后人摘步骤
- 修复：browser-smoke 补装 `[dev]`（TestClient 需要 httpx2）——`a17d26c`
- 双 OS 矩阵测试全绿（run 35255218410 success）

## P4｜F5 full76 满血验收与切换默认 ✅（`710efe3`+`8284332`，**工厂 v1.1.0 已发**）

- **v29 引擎 full76**：`acceptance --case all --engine v29` exit 0
  （demo2+full76 全流程，真实 76 只 75 图）
- **对 v2.7 基线逐字段比对**（`scripts/compare_full76_v29.py`）：
  硬门禁全过——CALIB 54 键逐键坐标一致、照片键集合一致、署名在位；
  差异 4 类（bio×76/area×5/features×5/coatGroup×31）定性为
  **数据源演进 + 人工文案/人工分类 vs 算法推导**，
  由 schools/nuc 的 `cats_override.json` 人工数据收口（F6 验证）
- **字节级终极验证**（`scripts/parity_nuc_package.py`）：schools/nuc 数据包直灌
  工厂渲染器 → HTML + 9 分片与 16 托管 `nuc/` **逐字节一致**
  （`docs/验收证据-F5-数据包字节对齐.md`）
- **默认引擎切 v29**（`ACTIVE_ENGINE` 缺省；v1 标 deprecated 保留一版）；
  10 个受影响旧用例按红线「先修适配」：v1 专属契约显式钉 `engine="v1"`，
  引擎无关用例改双引擎容错解析；`iter_chunks` 改生成器守住流式契约
- 四件套截图：开场/星图/影廊/护照/分享卡（`docs/screenshots-f5/`，
  分享卡含真实猫照与 v2.7 人工小传）
- **发版**：CHANGELOG.md + pyproject 1.1.0；tag v1.1.0 打在双 OS CI 绿的
  `8284332`；release https://github.com/TianWenzzzh/catgalaxy-factory/releases/tag/v1.1.0
- 对施工书的流程修正：full76 由授权代理自跑+证据回贴，「人类签字」以本报告
  本节为验收摘要，待人类复核确认

## P5｜F6 05 主仓库再基线 v2.8 ✅（05 `main@339b943` + tag v2.8）

- `scripts/export_v29_product.py`：工厂渲染器 × schools/nuc 数据包 → 正式产物
- **字节一致铁证**：05 `中北喵星图.html` sha256 `358e1e63…` == 16 托管
  `nuc/index.html`；9 分片逐一比对 ✓
- 门禁：「1 个 HTML 的内联块 + 9 个 .js = 14 块，失败 0」
- data/SKILL/README 未动；v2.7 人工资产零丢失（字节一致 + 逐字段双证明）
- 页面内版本串随字节对齐保持 v2.7 口径，仓库版本以 tag v2.8 为准（偏差已记录）

## P6｜F7 收口与反向 vendor ✅（16 `main@c839db3`+`e5eacd9`）

- `tools/starmap_render.py` 自工厂 `8284332` 反向 vendor（头部注明基线）；
  build.py 删除自持转义/数据块/分片渲染（约 130 行），只做数据装配与落盘
- **漂移/构建/懒加载/F11/往返全绿**——重构后 build 对 schools/nuc 仍与托管
  逐字节一致；新锁 `tests/test_vendor_snapshot.py` 钉定快照 sha256
- **16 v1.2.0**：CI 绿后打 tag + release（见文末「发版清单」）
- 三仓 README 关系链 + 施工书状态勾选 + pipeline skill 增补
  「跨三仓发版顺序：工厂 → 16 同步快照 → 05 直出」

## P7｜推广包 ✅（只备料未发布）

| 文件 | 内容 |
|---|---|
| `15/人工动作物料/05_校园共创招募_三平台版.md` | B 站长文/小红书/校园墙三版成稿 + 共性 checklist |
| `16/.github/ISSUE_TEMPLATE/school-application.md` | 种子学校申请模板（三条硬条件自查） |
| `16/docs/种子学校流程.md` | 申请→普查→构建→上墙四步 + 首批承诺 |
| `16/docs/10分钟教程.md` | 图文教程（克隆→数据包→一条命令→双击验收→换数据） |
| `16/gallery/index.html` 升级 | 「申请成为下一所点亮的高校」CTA + 三条路说明卡（数据驱动保持） |
| `16/docs/coze-workflow-design.md` | M3 Coze 工作流设计稿（两条落地路径 + 4 项上架验收） |
| `15/人工动作物料/06_Coze工作流上架申请草稿.md` | 提审成稿素材 |
| `15/人工动作物料/07_教程视频脚本_…md` | 60s 短版 + 3 分钟完整版分镜 |

门面：展示墙验活 HTTP 200；三仓 homepage 互链（16→gallery，工厂→meow，
05→gallery）+ topics 补齐。社交预览图上传为网页专属操作（gh api 不支持），
已留 `docs/screenshots-f5/` 素材，待人类在仓库 Settings 上传。

## P8｜终验

- 三仓状态与门禁终值见文末「最终汇总」
- 备份 bat 末次复跑记录见终报

## 事实修正（对任务书第 2 节）

1. 工厂 venv 重建后实际为 CPython 3.12.12（任务书预测对齐，非 3.14.6）✓
2. 16 仓 CI 双 job 的浏览器冒烟**比预期快**（52s），未构成时间盒压力
3. `backup` bat 三次运行全部零差异（今日无新照片），符合预期
4. 无其他与环境事实冲突的发现

## 未竟 backlog（按优先级）

1. **工厂 feat/v29-template 是否合回 master**（release 已从 feat 发出；
   master 暂留 v1 缺省以保「随时干净可用」——建议人类确认后 `git merge` + 推送）
2. full76 验收摘要人类签字（本报告 P4 节即摘要）
3. M2 招募三平台发布 + 首批学校筛选择期（物料已备，发布是人类动作）
4. Coze 工作流：按设计稿 §三 跑 4 项验收后走 A 路径（插件封装）提审
5. 社交预览图：仓库 Settings → Social preview 上传 `docs/screenshots-f5/f5-02`
6. 16 仓 gallery 的 covers 可换用 f5 截图风格统一（现用旧图，功能无损）

## 待人类动作

- 复核并「签字」P4 的 full76 验收摘要（有异议随时回滚：tag 均可删重打）
- 决定工厂分支合并策略（见 backlog 1）
- 发布招募帖 / Coze 提审（物料就绪）
- 可信时间戳申请（`04_可信时间戳申请清单.md`，v2.8 HTML sha256 已变，
  新值 `358e1e63…` 建议一并打戳）

## 迭代 1｜排版精修（「细到每一个字体」，2026-09-18）✅

- **机制化**：新增 `tools/make_v30_baseline.py`（TYPO_PATCHES 确定性补丁 +
  次数断言 + 四重自验）；v29 模板冻结为 `fixtures/v29_template.html` 历史快照，
  V30 现役往返 + 新鲜度门禁上线；`docs/设计规范-排版.md` 入库
  （117 处字号 / 22 字重 / 17 行高 / 44 字距全量审计 + 字体栈红线 + 变更流程）。
- **5 个补丁**：字体栈跨平台补全 ×2（苹方/雅黑后追加 HarmonyOS/MiSans/思源/Noto，
  macOS/Win 观感零回归）+ 计数器 `tabular-nums` ×3（开场统计/chips/图鉴进度）。
- **全链同步**：16 `cd7fb1c`（86 测试 OK，漂移绿，托管重建）→ 工厂 `b7cbf59`
  （模板 sha 同步 `56e3802c`，747/0）→ 05 `0d61848`（重导出，与 16 托管
  sha `bc3d3023` 一致，14 块门禁过）。
- **发版**：16 **v1.3.0**（tag 在 `cd7fb1c`）/ 工厂 **v1.2.0**（`b7cbf59`）/
  05 **v2.8.1**（`0d61848`）——三者 CI 均绿后打 tag。
- 迭代 2 候选（待截图评审后动）：字号归一档位评审、字距刻度文档化、
  gallery 同款字体栈升级。

## 迭代 2｜v31 字栈顺序修正（2026-09-18）✅ 文件级完成（git 提交待人类执行）

- **动机**：迭代 1 的字栈把 HarmonyOS Sans SC / MiSans 插在 Microsoft YaHei **之前**，
  装有这两款字的 Windows 设备（华为/小米本、设计师机）会从雅黑漂移到定制黑体，
  违反 `设计规范-排版.md` 自述红线「macOS/Windows 首选不变」——迭代 1 的
  「零观感回归」只在纯净 Windows 上成立。
- **2 个 U 补丁**（`tools/make_v31_baseline.py`，次数断言 + 四重自验，规则集不变）：
  U1a CSS 栈 / U1b SVG 栈，重排为
  `PingFang SC → Microsoft YaHei → HarmonyOS Sans SC → MiSans → 思源 → Noto`；
  Linux 兜底不损（雅黑在 Linux 不存在），macOS/Windows 回到真正零漂移。
- **代际切换**：v30 模板冻结为 `fixtures/v30_template.html`，V30 转历史回归，
  V31 现役往返 + v31 新鲜度门禁；`make_v30` 模板产物归属改指冻结快照
  （与 v29→v30 同款问题第二次出现，已把「上一代脚本改指冻结快照」写进
  `设计规范-排版.md` §六 流程）。
- **门禁实测**：16 仓 **102 项全绿**（4 个 golden 自证 skip 设计内；此前报告写
  86 系口径漂移，已统一为实测值），语法门禁 **65 块 0 失败**；工厂模板同步
  （sha `164cf054…`）后 **747 / 0 / 0 / 0**（junitxml 实测）；05 重导出与
  16 托管**逐字节一致**（均 `d56fdc84…`），14 块门禁 0 失败。
- **待人类执行**：三仓提交推送 + 发版（16 v1.3.1 / 工厂 v1.2.1+v1.2.2 / 05 v2.8.2），
  四段命令见总库根目录 `工厂仓故障与恢复步骤_20260918.md` 顶部——
  本总库自动化通道的 git 写入不可信（嵌套/覆盖式 ref 静默失败，见该文件故障记录）。
